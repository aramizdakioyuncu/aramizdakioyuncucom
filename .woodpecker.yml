kind: pipeline
name: armoyu-next-build

steps:
  - name: build-next-app
    image: plugins/docker
    settings:
      repo: armoyu-next-app
      dockerfile: ./Dockerfile-alpine # Senin dosyanın adı
      context: .
      build_args:
        - PROJECT_PATH=./next
