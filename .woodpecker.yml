kind: pipeline
name: armoyu-next-build

steps:
  - name: build-next-app
    image: plugins/docker
    settings:
      repo: armoyu-next-app
      dockerfile: Dockerfile
      context: . # Projenin ana klasöründen başla
